auth_enabled: false
stream_over_http_enabled: true    # HTTP OTLP = port 4318

server:
  http_listen_port: 3200
  http_path_prefix: /
storage:
  trace:
    backend: local
    wal:
      path: /var/tempo/wal
    local:
      path: /var/tempo/blocks