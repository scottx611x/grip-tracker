apiVersion: 1

datasources:
  - name: Tempo
    uid: tempo_ds
    type: tempo
    access: proxy
    url:   http://tempo:3200
    basicAuth: false
    isDefault: false
    editable: false

    jsonData:
      httpMethod: GET
      serviceMap:
        datasourceUid: tempo_ds
      nodeGraph:
        enabled: true